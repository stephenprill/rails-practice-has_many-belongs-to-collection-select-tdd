<div class="page-header">
  <h1><%= @cat.name %></h1>
</div>

<h3>This cat's toys are...</h3>

<div class="well">
    <ul>
    <% @cat.toys.each do |toy| %>
    <li><%= toy.name %> </li>
      <% end %>
  </ul>
  <em>Replace this with a list of the toys that belong to this cat</em>
</div>
